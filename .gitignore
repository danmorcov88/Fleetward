# Binaries
/bin/
/dist/
*.exe
*.test
*.out

# Go
vendor/
coverage.out
coverage.html

# Node
node_modules/
web/dist/
.vite/
# TypeScript incremental build state — machine-local, changes on every build
*.tsbuildinfo

# Local env & secrets — never commit
.env
.env.local
*.pem
*.key
!deploy/dev/**/*.key

# Editors / OS
.DS_Store
.idea/
*.swp

# Local dev data volumes
/.data/

# Local render of the GitHub wiki (make wiki). CI publishes the real one.
/.wiki/
