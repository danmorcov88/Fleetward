# Paths the documentation may name even though they do not exist.
#
# Format:  <repository-relative path>   <the one file allowed to name it>   # why
#
# Two rules, both deliberate:
#
#   A reason is mandatory. An allowance without one is a silenced error, and six months later
#   nobody can tell "deliberately mentioned" from "quietly wrong".
#
#   An allowance is scoped to one file. What makes a mention honest is the sentence around it —
#   "planned, gated on the Kubernetes sandbox provider" — and that sentence lives in one document.
#   A global allowance would extend it to every other file, which is how the same claim reappears
#   somewhere it is not qualified.
#
# docscheck refuses to load a malformed entry, and reports an entry nothing uses any more.

deploy/helm        CLAUDE.md                  # named in §3 as explicitly planned and gated on the Kubernetes sandbox provider; the rule above it says a directory is added by the slice that fills it
docs/slice-briefs  docs/dev/slices/README.md  # a git branch-name example (docs/<topic>), not a path — it collides with the docs/ directory by coincidence

# Slice B2 writes these. The allowances are scoped to the brief and become stale the moment the
# files exist, which is how they get removed: docscheck reports an allowance nothing uses any more.
plugins/sqlserver/plugin.go     docs/dev/slices/B2-sqlserver-plugin.md  # the slice's Files table, listing what B2 will create
plugins/sqlserver/conn.go       docs/dev/slices/B2-sqlserver-plugin.md  # the slice's Files table, listing what B2 will create
plugins/sqlserver/health.go     docs/dev/slices/B2-sqlserver-plugin.md  # the slice's Files table, listing what B2 will create
plugins/sqlserver/discover.go   docs/dev/slices/B2-sqlserver-plugin.md  # the slice's Files table, listing what B2 will create
plugins/sqlserver/backup.go     docs/dev/slices/B2-sqlserver-plugin.md  # the slice's Files table, listing what B2 will create
plugins/sqlserver/manifest.go   docs/dev/slices/B2-sqlserver-plugin.md  # the slice's Files table, listing what B2 will create
plugins/sqlserver/restore.go    docs/dev/slices/B2-sqlserver-plugin.md  # the slice's Files table, listing what B2 will create
plugins/sqlserver/verify.go     docs/dev/slices/B2-sqlserver-plugin.md  # the slice's Files table, listing what B2 will create
cmd/plugins/sqlserver/main.go   docs/dev/slices/B2-sqlserver-plugin.md  # the slice's Files table, listing what B2 will create

plugins/postgres/upload.go  docs/dev/slices/A4-backup-and-manifest.md  # written by A4 and correct then; slice B2 lifted the upload protocol into internal/plugin/sdk so a second engine could not drift from it, and a brief is left as written
